Here is the structured Skill Summary extracted from the provided solution:

### Data Processing & Target Alignment
- **Observation/Action:** Maps ground-truth strings to token-level start/end indices using `offset_mapping`, implements fallback substring matching for exact-match failures, and clamps indices to valid token bounds (excluding `[CLS]`/`[SEP]`).
- **ML Rationale (Why it works):** Subword tokenizers fragment words and attach punctuation, creating a spatial mismatch between raw character indices and model inputs. Offset mapping bridges this gap by preserving exact character-to-token boundaries. The fallback and clamping logic prevent gradient explosions from malformed targets, which are common in crowd-sourced annotations where annotators may include/exclude trailing spaces or punctuation inconsistently.
- **Guiding Principle:** Never assume 1:1 alignment between raw text spans and model tokens. Always build a robust alignment pipeline that uses offset mappings, handles tokenizer artifacts gracefully, and enforces structural validity (e.g., valid token ranges, non-null targets) before feeding data to the model.

### Feature Engineering & Input Representation
- **Observation/Action:** Uses raw tweet text as the sole input without explicit sentiment conditioning or hand-crafted features. Relies entirely on `roberta-large` contextual embeddings.
- **ML Rationale (Why it works):** Pre-trained transformers capture rich syntactic and semantic dependencies that inherently correlate with sentiment-bearing phrases. For short-form text like tweets, polarity cues are highly localized and contextually obvious, making explicit feature engineering redundant. The model implicitly learns to attend to sentiment-supporting tokens because the training pairs `(text, selected_text)` are already filtered by sentiment label.
- **Guiding Principle:** Prioritize contextual representation learning over manual feature construction for NLP tasks. Start with raw text + a strong pre-trained backbone, and only introduce explicit conditioning (e.g., injecting sentiment as special tokens) if ablation studies show the base model fails to capture domain-specific signals.

### Model Architecture & Training Dynamics
- **Observation/Action:** Employs a dual-head architecture (start/end linear layers) over a `LayerNorm`-stabilized RoBERTa backbone. Trains with label smoothing (0.1), gradient accumulation, AMP, gradient clipping, and enforces `start ≤ end` via logit masking during inference.
- **ML Rationale (Why it works):** Decomposing span extraction into two independent token classification tasks simplifies the optimization landscape. `LayerNorm` before the heads stabilizes activation distributions during fine-tuning. Label smoothing mitigates overconfidence on noisy crowd-sourced spans, improving calibration. Gradient accumulation + AMP simulates larger batch training, reducing gradient variance. The `end_logits` masking enforces the physical constraint of a valid span, directly aligning predictions with the contiguous nature of the target and preventing metric-penalizing impossible outputs.
- **Guiding Principle:** Frame structured prediction tasks as constrained sub-problems with logical decoding rules. Use architectural stabilizers (LayerNorm, label smoothing) and training techniques (AMP, accumulation, clipping) to maintain optimization stability when fine-tuning large models on small/noisy datasets. Always enforce task-specific logical constraints in the decoding space to align predictions with evaluation metrics.

### Validation & Evaluation Strategy
- **Observation/Action:** Uses 3-fold StratifiedKFold on `sentiment`, computes Jaccard directly on validation sets, averages logits across folds for test predictions, and includes a heuristic train/val Jaccard gap audit.
- **ML Rationale (Why it works):** Stratification on `sentiment` preserves the distribution of polarity classes across folds, preventing biased splits that could skew span extraction performance (since sentiment dictates span length and lexical characteristics). Logit averaging preserves probabilistic uncertainty across models, yielding smoother, more robust boundary predictions than hard-voting decoded strings. The gap audit provides a metric-aligned diagnostic: Jaccard is highly sensitive to exact boundary matches, so a large train/val gap directly indicates memorization of noisy spans rather than generalizable linguistic patterns.
- **Guiding Principle:** Align validation strategy directly with the evaluation metric and the latent drivers of the target distribution. Stratify on high-level labels that influence target structure. For ensembles, average outputs in logit/probability space before decoding to preserve uncertainty. Implement metric-specific overfitting diagnostics rather than relying solely on loss curves.