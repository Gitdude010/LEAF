Here is the structured Skill & Insight extraction for the provided Spaceship Titanic solution:

### 📊 Data Processing
- **Observation/Action:** Concatenates train and test sets before imputation and encoding. Fills categorical missing values with `"Unknown"` and numerical features with the global median.
- **ML Rationale (Why it works):** In this dataset, missing values are **not Missing Completely At Random (MCAR)**. For example, passengers in `CryoSleep=True` cannot access amenities, so `RoomService`, `Spa`, etc., are systematically missing. Using `"Unknown"` preserves missingness as a discrete state that the model can learn to associate with cryosleep or data corruption zones. Median imputation provides a robust central tendency that avoids skewing distributions caused by extreme luxury spending outliers.
- **Guiding Principle:** Always diagnose the *missingness mechanism* (MCAR vs MAR vs MNAR) before imputing. When missingness is structurally tied to other features, treat it as an explicit category or derive conditional imputation rules rather than applying blanket statistical fills. Fit encoders/scalers on the combined dataset to guarantee identical latent space mappings across train/test splits.

### 🔧 Feature Engineering
- **Observation/Action:** Decomposes `PassengerId` into `GroupId`, splits `Cabin` into `Deck`, `CabinNum`, `Side`. Creates `GroupSize`, `TotalSpend`, and `SpendingRatio` (RoomService / Total).
- **ML Rationale (Why it works):** `PassengerId` encodes a hidden social graph: passengers traveling together share physical proximity and fate during the anomaly. Extracting `GroupId` allows the model to implicitly learn group-level transport patterns. `Cabin` parsing converts an opaque string into spatial coordinates (`Deck` = vertical layer, `Side` = port/starboard), which strongly correlates with impact zones of the spacetime anomaly. `SpendingRatio` transforms highly correlated absolute spending columns into a behavioral preference metric, reducing multicollinearity while capturing passenger lifestyle/destination intent.
- **Guiding Principle:** Treat composite IDs and structured strings as compressed relational data. Decompose them into atomic, semantically meaningful components (spatial, temporal, social). When features measure the same underlying construct (e.g., multiple spending types), derive *relative* or *ratio-based* features to isolate behavioral signals from magnitude noise and improve model conditioning.

### 🧠 Model Architecture
- **Observation/Action:** Implements an FT-Transformer with separate categorical embeddings, linear projections for numerical features, a learnable `[CLS]` token, and a Transformer Encoder backbone.
- **ML Rationale (Why it works):** Tabular data often contains high-order, non-linear interactions (e.g., `Deck=S` + `CryoSleep=False` + `High_VRDeck_Spend` → high transport probability). FT-Transformer projects heterogeneous inputs into a unified `d_model` space, allowing self-attention to dynamically weigh feature interactions without manual cross-product engineering. The `[CLS]` token acts as a global aggregator, pooling localized feature signals into a unified decision representation. Despite the dataset's modest size (~13k rows), the architecture succeeds because strong regularization (dropout, weight decay, label smoothing) prevents memorization while attention captures latent spatial/behavioral manifolds.
- **Guiding Principle:** For structured data with mixed types and suspected complex feature dependencies, use architectures that natively handle heterogeneity (e.g., Feature Tokenization + Attention). Self-attention replaces manual interaction engineering by learning dynamic, context-dependent feature weights. Pair expressive architectures with aggressive regularization when sample size is limited to maintain the bias-variance tradeoff.

### 🛡️ Validation & Training Strategy
- **Observation/Action:** Uses 5-fold StratifiedKFold, CrossEntropy with `label_smoothing=0.1`, gradient accumulation, AMP, early stopping, and explicitly monitors the Train/Val accuracy gap during training. Final predictions use probability averaging across folds.
- **ML Rationale (Why it works):** StratifiedKFold preserves class distribution in each fold, ensuring stable accuracy estimation on a binary task. Label smoothing penalizes overconfident logits, improving probability calibration and robustness to label noise or edge cases. Gradient accumulation simulates larger effective batch sizes, stabilizing gradient variance for attention-based models. The explicit `gap = train_acc - val_acc` audit provides real-time overfitting diagnostics, allowing dynamic intervention (e.g., adjusting dropout/early stopping). Probability averaging reduces fold-specific variance and yields smoother decision boundaries than hard voting.
- **Guiding Principle:** Implement a *diagnostic training loop* that tracks generalization metrics (train-val gap, calibration error) alongside primary loss. Use label smoothing and gradient accumulation as standard regularization tools for modern architectures. Always ensemble via soft probability averaging to preserve model uncertainty and improve final metric stability. Early stopping should be coupled with explicit overfitting thresholds, not just loss plateaus.